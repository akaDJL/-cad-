# 领域配置: rebar_auto
# 此文件由 auto_registry 自动扫描加载，新增领域只需在此目录放入 YAML 文件
# 无需修改 cli.py 或 knowledge/__init__.py

name: "rebar_auto"
module: "envcad.standards.rebar_auto"
description: "钢筋详图+配料表联动"
functions:
  beam_schedule: "draw_beam_with_schedule"
  column_schedule: "draw_column_with_schedule"
