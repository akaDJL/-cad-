# 领域配置: ecology
# 此文件由 auto_registry 自动扫描加载，新增领域只需在此目录放入 YAML 文件
# 无需修改 cli.py 或 knowledge/__init__.py

name: "ecology"
module: "envcad.standards.eco"
description: ""
functions:
  noise_contour: "draw_noise_contour"
